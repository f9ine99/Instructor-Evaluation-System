-- ============================================================
-- HOPE Instructor Evaluation System — no bundled demo data
-- ============================================================
-- Previously this file inserted sample departments, users, courses,
-- enrollments, evaluation sheets, and submissions. That has been removed
-- so you can add only the test data you need (Admin UI or custom SQL).
--
-- Run `database/schema.sql` once to create tables and load the 13 default
-- evaluation question templates (required when deans create new sheets).
--
-- To clear an existing database of all transactional/org data but keep
-- the default question templates:
--   mysql ... < database/clear_data.sql
--
